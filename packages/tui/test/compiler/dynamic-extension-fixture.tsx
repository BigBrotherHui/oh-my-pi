import "./memory-renderer";

/** Component loaded from a runtime-selected TSX extension path in compiled-build smoke coverage. */
export function DynamicExtensionFixture() {
	return <box>runtime extension</box>;
}
